FROM mouyu/medicalai:latest

RUN pip install --no-cache-dir "vllm==0.26.0" \
    && pip install --no-cache-dir "numpy<2" \
    && pip uninstall -y flash-attn

ENV VLLM_USE_FLASHINFER_SAMPLER=0 \
    VLLM_WSL2_ENABLE_PIN_MEMORY=1
